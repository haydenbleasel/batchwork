---
"batchwork": patch
---

Bump AI SDK dependencies to the v4 provider spec: `@ai-sdk/anthropic`, `@ai-sdk/google`, `@ai-sdk/openai`, `@ai-sdk/mistral` and `@ai-sdk/xai` to `^4`, `@ai-sdk/groq` to `^4`, `@ai-sdk/togetherai` to `^3`, and the `ai` core to `^7` (required so the core recognises the new `LanguageModelV4`/`EmbeddingModelV4`/`ImageModelV4` specification).
